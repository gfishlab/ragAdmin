# 任务结束后 Hook

## 目标

让任务完成后同步完成最小复核、冲突检查和必要沉淀。

## 默认动作

1. 检查是否完成预期验证
2. 检查文档、代码、Schema 是否出现新冲突
3. 判断本次任务是否形成新观察或稳定结论
4. 如有需要，更新 `memory`

## 关注点

- 本次任务是否暴露了新的高频风险
- 本次任务是否形成了可跨任务复用的经验
- 本次任务是否触发了流程层面的演进
